import React from 'react';
import styled from 'styled-components';

const PageTitle = () => (
    <AppTitle>Breath Relaxer</AppTitle>
);

const AppTitle = styled.h1`
    text-align: center;
`


export default PageTitle;


